name: release

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build Archives
        run: bash ./build.sh
      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: Release
          path: scriptor_*.zip